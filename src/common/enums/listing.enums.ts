export enum ListingStatus {
  ACTIVE = 'active',
  PENDING = 'pending',
  EXPIRED = 'expired',
  DRAFT = 'draft',
  REJECTED = 'rejected',
}

export enum ListingPackage {
  STANDARD = 'standard',
  VIP = 'vip',
  DIAMOND = 'diamond',
}

export enum ListingPurpose {
  SALE = 'sale',
  RENT = 'rent',
}
